This test includes a single point energy calculation for a water sphere of size 20A. Run
the test with the following command:

python ../../../bin/qdyn.py -t topo.json -m eq1.inp -d TEST --verbose

Then run compare.py to check if the obtained results match the reference results.
